<h1><%= @category.name %></h1>

<p><%= @category.description %></p>

<ul>
  <li><%= link_to "Edit", edit_api_v1_category_path(@category) %></li>
  <li><%= link_to "Destroy", api_v1_category_path(@category), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li>
</ul>
